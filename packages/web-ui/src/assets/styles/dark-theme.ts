import createTheme from "./base-theme";

const muiTheme = createTheme({
    palette: {
        type: "dark",
        primary: {
            main: "#33658A",
        },
        background: {
			paper: "#1C2931",
            default: "#2F4858",
        },
    },
});

export default muiTheme;
